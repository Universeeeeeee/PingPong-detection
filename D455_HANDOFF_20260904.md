# D455 球桌与乒乓球检测交接说明

版本日期：2026-09-04。适用项目：`pc:/home/pc/rsy/predict_node/`。

**当前状态：完整相机程序已部署；乒乓球二维检测在开发录像上较好，实时三维检出率 95% 尚未达到，三维绝对精度尚未验收。** 本文按当前源码整理。旧 README、初次部署报告和原始压缩包中的算法说明可能已过时，应优先看本文。

## 1. 接手时先分清三个入口

| 用途 | 入口 | 当前实现 / 定位 |
|---|---|---|
| 连接 D455，同时检测球桌与乒乓球 | `run_d455_tracker.sh` | 完整主程序；输出二维球、三维球、球桌坐标及视频 |
| 采集实验数据 | `record_d455_motion.sh` | 保存 RGB、左右 IR、深度及时间信息；录制成功不代表检测成功 |
| 用录像模拟实时三维检测 | `test_d455_realtime.sh` | 实验分支；默认 NCC，可选 FFS；未替换完整相机程序 |

单独二维显示入口：`run_d455_2d.sh`。原有 `~/rsy/table_detect` 和 `~/rsy/event_based_ping_pong_ball_trajectory_prediction` 是其他程序，不是本项目入口。

- 服务器目录：`/home/pc/rsy/predict_node`。
- Windows 副本及本交接包：`F:\codex\d455_deployment`。
- 启动器默认 Python：`/home/pc/anaconda3/envs/isaac/bin/python`，不要求先 `conda activate/deactivate`。
- 核心依赖：Python、NumPy、OpenCV、pyrealsense2、pyzmq；服务器已配置。FFS 实验另用项目下 `.venv_ffs/bin/python` 和 RTX 5090。
- 历史已验证依赖版本：NumPy 1.26.4、OpenCV 4.11.0.86、pyrealsense2 2.58.3.10794、pyzmq 27.1.0；每次实际版本以 `runs/.../run.json` 为准。
- 图形窗口必须在 pc 图形桌面终端运行；无图形 SSH 去掉 `--display`。

## 2. 球桌检测：外轮廓 + 深度平面 + 已知尺寸

实际入口是 `table_pose_geometry.py` 的 `FixedTablePoseTracker`。主程序将它导入为 `TablePoseTracker`。主程序内保留的 `LegacyTablePoseTracker`、旧 SE(3) 外推代码不代表当前启用算法。

处理过程：

1. 在 RGB 中提取蓝色台面区域，找到可见的实际外轮廓和边线。
2. 用有效台面深度拟合平面，将图像边界转换为米制平面上的约束。
3. 根据已知长宽匹配矩形，估计平面内位置与朝向。四条外边支持充分时，也可由边线交点生成平面 PnP 候选。
4. 对候选检查边线像素支持、边界内外颜色变化、支持点分布、台面覆盖、前方深度和独立深度残差。中线、背景线或凸包补出的边不能直接作为真实外边。
5. 默认连续 5 次一致观测才确认。确认后固定姿态，以后只验证，不随逐帧边线噪声和运动外推漂移。
6. 验证失败时拒绝更新，进入历史保持或失效状态，再独立重检测。

状态：`SEARCHING → CANDIDATE → VALID`；失配可进入 `HOLDING / LOST`。历史框能显示，不代表当前球桌坐标有效。

**缺角怎么办：** 四条边线仍有支持时可通过交点恢复缺角；只有三条外边但约束充分时，利用已知尺寸恢复矩形。首次只看到两条平行边时拒绝初始化。初始化后部分遮挡可继续验证；完全看不见时不能无限期维持有效坐标。

| 参数 | 启动器默认值 / 作用 |
|---|---|
| `--table-length` / `--table-width` | 2.74 / 1.525 米；半桌长可设 1.37 |
| `--table-hsv-low` / `--table-hsv-high` | `85,80,40` / `135,255,255` |
| `--table-min-area` | 15000 像素 |
| `--table-confirm-frames` | 5 次一致观测 |
| `--table-validation-hz` | 验证频率上限 12 Hz，不保证初始化也达到此速度 |
| `--table-hold-seconds` | 0.6 秒历史显示保留；不等于有效世界坐标保留 |
| `--table-edge-tolerance` | 5 像素 |

前提是相机与球桌相对固定。移动相机后需重新确认；未接入机器人头部运动补偿。新球桌坐标系会递增 `table_frame_id`，下游不能跨版本直接拼接球轨迹。

## 3. 乒乓球检测：完整主程序正在使用什么

### 3.1 二维 RGB 检测

默认橙球使用 `ball_image.py::ImageBallDetector`，是颜色、形状和运动特征方法，**没有默认运行 YOLO、BlurBall 或其他神经网络球检测器**。

- 先用较严格球色提供种子，再重建完整颜色区域，避免把篮子、衣物或包装上的小色块当完整小球。
- 橙球在蓝桌背景上的拖影可能偏红/粉，因此包含扩展色域与色度分割；这些弱色域必须有更强运动及关联证据。
- 用背景颜色变化和帧差寻找新出现的运动区域；同时保留满足外观要求的静止球。
- 检查轮廓面积、短轴宽度、长宽比、凸实度、填充率、圆度、周围同色比例、截断情况及颜色强度。
- 清晰球强调紧凑形状；拖影球允许长条形，宽度主要看短轴，不强行要求圆。
- 利用历史位置和速度关联候选；弱候选只允许短时续接已有身份，歧义大时返回无球。
- 相同或倒序 RGB 时间戳不重复处理。

`--rgb-detector auto` 默认对橙球用上述方法，白球/黄球仍用 `HSVBallDetector`；可用 `--rgb-detector hsv` 做旧链路对照。因此橙球测试成绩不能直接套到白球。

### 3.2 三维测量

完整程序中的 `IRBallDetector` 在左右红外图中提取局部亮目标和运动候选，通过预测 ROI 与 RGB 校准视线范围限制搜索，再由 `StereoCandidateSelector` 选择配对。

候选必须通过左右几何关系、三角化、重投影误差、图像相似性、实际尺寸、匹配歧义和可用 RGB 身份一致性检查；全部不合格就无三维测量。缺少同步 RGB 身份时还检查当前 IR 目标是否具有正亮度对比，防止差分暗影或上一帧残影被续接为球。

第二个测量来源是 RGB 对齐深度，但必须满足时间接近、前景有效像素与离散度、背景混入及物理尺寸检查。**用球直径反推的深度不能再反过来证明它是球，也不计为独立三维实测。**

延迟 RGB 可以在缓存的原始 IR 中查找相近时刻配对。测量交给有限历史融合器，并按来源与时间戳去重，不能因同一 RGB 在多个 IR 周期中被读取而反复提高置信度。

### 3.3 确认、滤波与短时预测

- `ball_validation.py::BallTrackGate`：候选准入、默认 3 个独立观测确认、身份关联、失配拒绝、丢失重检。
- `ball_motion.py::TimestampedBallFilter`：状态为 `[x,y,z,vx,vy,vz]`，相机坐标系中的常速度卡尔曼滤波；按测量协方差加权，用创新门限拒绝异常更新。有限历史窗口约 0.20 秒，用于处理晚到测量，不要求所有输出等 0.20 秒。
- 默认短时预测上限 0.12 秒。预测仅维持短时间输出，不算新检测。
- 当前实际运行模型为 `camera_constant_velocity_no_contact`，自动球桌接触/反弹预测已关闭。旧类中保留的重力与反弹代码、保留的恢复系数参数不是当前启用功能；当前链路也不是原 BlurBall 项目中的 IMU + EKF + 空气阻力预测。
- 候选分数/置信分数是排序和状态依据，未标定为真实概率。

## 4. 最新实时实验分支与主程序的区别

`replay_fast_rgbd.py → fast_rgbd.py → ball_image_roi.py + ball_epipolar.py`：

- RGB 局部快速检测，失配后全图重检。
- RGB 引导局部 IR 候选搜索，以 NCC（归一化互相关）匹配左右图块，亚像素修正后进行三角化与形状、尺寸、几何验证。
- **XYZ 全部来自同一对 IR 观测，不再将 RGB 射线与邻近时刻的 IR 深度拼接。** 左右 IR 时间差要求 ≤1 ms，RGB 与 IR ≤18 ms；记录各自时间戳。
- 同一 IR 对只用一次；不允许旧 IR 测量使三维时间倒退。
- 按原始主机到达时间以墙钟 1 倍速送帧，RGB 队列容量为 1，过期工作放弃，统计排队/处理延迟和丢帧。
- 不包含完整球桌估计、生产滤波发布、画面显示和编码；预解码与 SDK 对齐成本也未纳入。不能用此实验成绩代表完整程序实机端到端成绩。

可选 FFS：`ball_foundation.py + ffs_runtime.py` 使用 Fast-FoundationStereo 提供视差提示，之后仍通过 NCC 和候选验证。使用 NVIDIA 官方 bp2 权重、FP16、384×256 裁剪、4 次迭代；不是独立的红外球识别模型，未使用 TensorRT。本次覆盖率更低，所以 **FFS 未设为默认**。模型在实际工作线程预热，启动约 1.05 秒单独统计，修复了首帧停顿丢帧。

详情及权重来源见随包 `IR_XYZ_FFS_STATUS.md`。红外小模型尚未训练；服务器已生成 63 组待复核样本，位于录制目录下 `ir_review_20260904/`，标签初始全部未复核。

## 5. 已有成绩：不同分母不能混在一起

主要开发录像：`recordings/20260903_203939_366112_rally`。正式动作 15 秒；含预热共约 18.18 秒，545 RGB 帧、1632 IR 周期。正式区间 450 RGB 帧，其中 398 可定位，50 不可见，2 可见性不确定。

| 版本 / 指标 | 结果 | 如何解释 |
|---|---|---|
| 主程序已接入的 RGB 二维检测 | 392/398 = 98.49%；另有 1 个错误输出 | 开发片段、RGB 人工复核中心，约 10 px 容差；不是独立数据集成绩 |
| 完整主程序三维离线融合 | 199/1349 = 14.75% 实测更新周期 | 分母为所有正式 IR 周期，含遮挡/无球；不是可见球召回率，也不是实时回放成绩 |
| 同一完整主程序含短时预测输出 | 638/1349 = 47.29% | 包含预测，不能当实测检出率 |
| 最新 NCC 实时实验，50 ms 内 | 299/398 = **75.13%** | RGB 身份位置匹配且有通过几何检查的三维输出 |
| 最新 FFS + NCC 实时实验，50 ms 内 | 216/398 = **54.27%** | 同一开发片段；未替代 NCC |
| NCC / FFS 三维输出延迟 P95 | 44.18 / 47.94 ms | 两者该次回放 RGB 均无丢帧；不含完整生产功能 |

NCC 的 99 个未及时成功帧中，94 帧已有二维位置但无通过验证的双目结果，5 帧未找到二维球。FFS 只额外补回 1 帧，离线并集 300/398，不是已实现的实时融合成绩。要在此分母上达到至少 95%，至少需要 379 个及时成功帧。

**没有独立左右 IR 球心标注和公制三维真值，75.13% 只能称当前口径的三维输出覆盖率，不能声称三维正确检出率或精度已经达标。** 暂定时限为 50 ms，少量响应仍可能超时，不能承诺硬实时。

球桌已有 11 项回归和 10 帧 RGB-D 回放验证；曾观察到约 7.7 mm 留出深度残差，它是内部一致性指标，不是真值误差。完整球检测/融合版本历史 54 项相关测试通过；最新实时实验 17 项测试通过。尚缺独立场景的完整实时与绝对坐标验收。

## 6. 可直接使用的命令

以下均在 **pc** 上执行。其他同学远程使用时先 `ssh pc`（或自己的服务器地址）。不要同时运行多个占用 D455 的程序。

### 完整检测

```bash
cd ~/rsy/predict_node
# pc 图形桌面终端；无图形 SSH 去掉 --display
./run_d455_tracker.sh --duration 30 --display \
  --ir-exposure-us 2000 --ir-gain 64
```

2000 μs / gain 64 是目前运动录像用过的配置，不承诺所有光照下最优。省略 IR 曝光参数时主程序默认 800，增益未指定时沿用设备值；RGB 由启动器设为自动曝光。IR/depth 请求 848×480@90，RGB 1280×720@30，实际档位以运行记录为准，可能回退。

正常退出按一次 Ctrl+C 并等待视频封装。窗口快捷键：`q`/Esc 退出、`r` 重置球跟踪、`t` 球桌重检测、`s` 保存球桌位姿。保存的位姿是初值，加载后仍需验证：

```bash
./run_d455_tracker.sh --load-table-pose --display
./run_d455_tracker.sh --table-length 1.37 --display
./run_d455_tracker.sh --help
```

半桌参数适用于确实以半桌为检测对象的场景，不应为了让错误矩形看起来吻合而随意改尺寸。

### 录制与数据验证

```bash
cd ~/rsy/predict_node
./record_d455_motion.sh --duration 15 --scene rally --display \
  --ir-exposure-us 2000 --ir-gain 64
```

`rally` 表示对打/连续运动场景标签，不是算法模式。其他标签有 `bounce`、`fast`、`occlusion`、`no_ball`、`static`、`smoke`。先保持相机固定，等待默认 3 秒预热和 `[READY]` 后开始动作；15 秒不包含预热。

录制结束检查 `[DONE] ... status=complete` 和目录内 `camera.json`。持续 `stream frames={}`、流中断或 `status=failed/interrupted` 都不能当作完整采集。已加入启动等待、四路流健康检查、至多一次启动恢复及停止封装处理。

```bash
# 将下面目录换为刚录好的实际目录
/home/pc/anaconda3/envs/isaac/bin/python export_d455_recording.py \
  recordings/20260903_203939_366112_rally --verify-only
```

新录像需要导出数组才能进入实验回放；去掉 `--verify-only` 执行导出。导出不等于自动生成球可见性真值标签。定量评分还需要相应 `analysis/rgb_2d_audit/labels.json`，不能照搬旧视频标签。

### 实时回放

```bash
cd ~/rsy/predict_node
./test_d455_realtime.sh --deadline-ms 50
# 可选对照，服务器需要已有模型及 .venv_ffs
./test_d455_realtime.sh --stereo-backend ffs --deadline-ms 50
```

回放不打开物理相机。新结果写入 `realtime_runs/<时间>_<PID>/`，其中 `summary.json` 为分数和延迟，`frames.json` 为逐帧决策。新录像用 `--recording <目录>` 指定，前提是导出和评分标签已准备好。

## 7. 输出、可视化与下游接口

完整检测每次新建 `runs/<时间_随机后缀>/`：

- `ball_overlay.mp4`：球、轨迹及状态；`table_pose.mp4`：球桌框与状态。
- `telemetry.jsonl`：球、球桌和诊断；`run.json`：实际命令、版本与文件哈希；`console.log`：日志。
- 最后一帧调试 JPG：有对应图像时生成。

录制目录 `recordings/<时间_场景>/`：`capture.db3` 是原始多流录制，`frames.jsonl` 是流时间/帧号元数据，`camera.json` 是参数与健康结果，`preview.mp4` 是预览。**仅保存 MP4 不足以重建本项目的双目与深度实验。** 正常停止后在 pc 桌面用 `vlc <实际视频路径>` 播放。

画面中青色十字为二维图像观测，绿色圈为三维确认且本周期有实测更新，紫色圈为短时预测。球桌绿色为当前有效，灰色为历史保持，红色为待确认，带 `*` 的角为推算角。以数据字段为准，不以“画了圈/框”判断检测成功。

### 坐标与字段

| 字段 | 语义 |
|---|---|
| `ball_2d.uv` | RGB 像素 `(u,v)`，左上原点；对应它自己的 `timestamp_s` |
| `ball_2d.new_observation` | 本 RGB 帧首次发布；重复发布不可重复计测量 |
| `ball_2d.depth_valid` | 二维输出自身不承诺深度，当前为 false |
| `ball.position_camera_m` / `velocity_camera_mps` | 左 IR 光学系，单位米 / 米每秒；x 右、y 下、z 前 |
| `ball.camera_valid` | 相机系轨迹可用；可能包含短时预测 |
| `ball.measured_this_frame` | 本处理周期有新的已接纳观测；不代表发布坐标未经滤波，也不保证恰好在当前周期曝光 |
| `ball.measurement_source` / `measurement_age_s` | 来源与观测年龄，用于区分实时观测和保持 |
| `ball.world_valid` | 球桌系是否可用；false 时不能消费球桌系坐标 |
| `ball.position_table_m` / `velocity_table_mps` | 球桌系结果；球桌失效时为 null |
| `table.T_camera_table` | 球桌系 → 左 IR 相机系的齐次变换 |
| `table.T_table_camera` | 上述逆变换；平移列是相机在球桌系的位置 |
| `table.position_camera_m` | **球桌中心在相机系的位置**，不是相机在球桌系的位置 |
| `table_frame_id` | 球桌坐标系版本，重检测改变后下游需断开旧轨迹关联 |

球桌原点在台面中心，x 沿长边、y 沿短边、z 朝上；代码通过相机侧约定解决矩形 180° 对称性。接机器人前需核对轴向与机器人侧，矩形视觉本身不能识别哪位选手是“对手”。当前无机器人 base 系标定。球的旋转矩阵是沿速度定义的运动坐标架，不是球自旋测量。

主程序 ZMQ 默认发布 `tcp://*:5555`，消息使用 Python `send_pyobj`。Python 接收示例：

```python
import zmq
ctx = zmq.Context()
sub = ctx.socket(zmq.SUB)
sub.connect('tcp://127.0.0.1:5555')  # 异机替换为 pc 的实际 IP
sub.setsockopt(zmq.SUBSCRIBE, b'')
while True:
    msg = sub.recv_pyobj()
    b = msg['ball']
    if b['camera_valid'] and b['measured_this_frame']:
        print(msg['timestamp_s'], b['position_camera_m'], b['measurement_source'])
```

C++/ROS 接手时需另加序列化/消息适配；本项目当前不是 ROS node，实验 `frames.json` 也不是主程序 ZMQ 协议。`recv_pyobj` 仅用于可信发布端。

## 8. 文件导航与接手顺序

| 文件 | 维护职责 |
|---|---|
| `d455_table_tennis_tracker.py` | 采集、线程、RGB/IR融合、主循环、ZMQ/JSONL、显示 |
| `table_pose_geometry.py` | 固定球桌初始化、缺角、验证、失效重检测 |
| `ball_image.py` | RGB 完整颜色区域、外观与运动、二维身份 |
| `ball_validation.py` | 独立深度/尺寸证据、候选准入、三维确认状态 |
| `ball_motion.py` | 时间戳测量、协方差、历史融合、常速度 KF |
| `record_d455_motion.py` / `recording_health.py` | 原始录制、流计数、恢复、健康诊断 |
| `export_d455_recording.py` | SDK 回放核验与数组导出 |
| `fast_rgbd.py` / `ball_epipolar.py` / `ball_image_roi.py` | 低延迟实验检测路径 |
| `realtime_source.py` / `replay_fast_rgbd.py` / `replay_realtime_3d.py` | 因果墙钟回放、队列、评分 |
| `ball_foundation.py` / `ffs_runtime.py` | 可选 FFS 视差提示，非默认 |
| `prepare_ir_review.py` / `ir_review_template.html` | 离线红外复核包生成，不能直接当训练真值 |

建议先按命令运行现有录像，检查 `summary.json`；再连接相机看二维与三维状态；最后接收坐标接口。不要先放宽几何/身份门限来追求更高输出比例。

下一阶段重点：复核红外球心与可见性，补充拖影、背景干扰和遮挡样本；用独立录像划分训练/测试，尝试小型 IR 球心检测器；保留双目几何及重复帧拒绝；以正确实测、误检、位置误差及 P95/P99 延迟共同验收。已有 63 组样本只是标注起点，尚不足以证明能训练到 95%。

## 9. 交接包范围与版本核对

随包包括本文、关键源码、启动/录制/回放脚本、相关测试、最新实验说明和 SHA256 清单。不含 Python 环境、第三方 FFS 源码及模型大权重、原始录像、历史 runs，也不含自动安装/覆盖部署脚本；它是源码快照，在已配置的 pc 项目中按本文复现。FFS 的第三方源码和权重在服务器 `third_party/` 下，跨机器复现需另行准备。

本次已对照服务器与本地的 9 个核心文件，哈希一致。正式主程序 SHA256：
`f08b257ae124371ad73b2e0abe623261e7a0090cfda7b6af3c356a8cf939d179`。

源码里保留 Legacy 类用于对照，不应仅凭类存在认定算法启用。旧报告中的“77.4%覆盖率”“89.6 FPS”“EKF/重力反弹”等来自不同版本或统计口径，不能写进当前交付验收指标。
