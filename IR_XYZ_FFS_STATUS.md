# D455：同步 XYZ 修正与学习型双目实测

2026-09-04。**实时三维检出率 95% 尚未达到。默认保留 NCC。**

本轮在服务器 `/home/pc/rsy/predict_node` 的实时回放实验分支完成修改、部署和对照测试。正式相机主程序 `d455_table_tennis_tracker.py` 未替换，避免将未达标实验接入现场。

## 已完成的修改

1. `fast_rgbd.py`：XYZ 三个分量现在全部取自同一对左右红外观测的三角化结果，时间戳取左红外采集时刻，坐标系为 `d455_left_ir_optical_frame`。RGB 仅参与身份关联，单独记录其时间戳，不再将 RGB 射线与另一个时刻的 IR 深度拼成 XYZ。左右红外时间差上限 1 ms，RGB 与 IR 上限 18 ms；未做同时曝光的虚假假设。
2. 拒绝重复或早于上一三维更新的 IR 帧；位移检查使用实际 IR 时间差。评分仍按 RGB 身份观测关联标签，避免 XYZ 时间戳改变后误关联下一 RGB 帧。
3. `ffs_runtime.py`、`ball_foundation.py`：增加可选 Fast-FoundationStereo 视差提示，由原来的 NCC、轮廓、尺寸和几何检查继续验证。原始红外亮斑仍是左目候选来源，并不是已训练的红外球检测器。
4. `replay_fast_rgbd.py`：在实际处理线程预热 CUDA 后才启动回放，修复首帧约 0.85 秒停顿和 24 帧丢失。预热约 1.05 秒单独记录，不隐藏到逐帧延迟中。
5. `test_d455_realtime.sh` 增加 `--stereo-backend ffs`；默认仍为 NCC。FFS 使用独立 `.venv_ffs`，未改全局 Python 环境。
6. 17 项回归测试通过，包括完整 IR XYZ、时间顺序、标签关联、缺失右球不能靠模型提示通过、无效视差不得退回无约束匹配、过期预算不启动模型、未校正相机拒绝进入该后端。

旧拼接 XYZ 与同一输出选中的 IR 三角化 XYZ 之间，差异中位数 1.51 cm、P95 6.13 cm、最大 9.23 cm。这个数值说明被消除的坐标不一致量级，**不是已验证的真实三维误差改善**。

## 同一录制的实时对照

录制 `20260903_203939_366112_rally`，正式 15 秒，398 个 RGB 球可见帧；50 ms 为本轮暂定时限。按记录的到达时间以墙钟 1 倍速回放，只读取已到达图像；预测、重复深度、球尺寸反推深度均不计实测。

| 指标 | 修正后的 NCC | FFS 提示 + NCC 验证，工作线程预热后 |
|---|---:|---:|
| 50 ms 内三维输出 | 299/398 | 216/398 |
| 及时三维输出覆盖率 | **75.13%** | **54.27%** |
| 全部匹配三维输出 | 299 | 217 |
| 三维输出延迟 P95 | 44.18 ms | 47.94 ms |
| 三维输出最大延迟 | 49.96 ms | 52.00 ms |
| 所有 RGB 响应最大延迟 | 60.87 ms | 52.96 ms |
| RGB 处理 / 丢帧 | 545 / 0 | 545 / 0 |
| 含录制预热片段的实际回放时长 | 18.20 s | 18.20 s |

FFS 仅补回 NCC 未及时覆盖的 1 帧 `rgb_0199.npz`。两者结果的离线并集为 300/398（75.38%）；这个并集不是已经实现的实时融合算法成绩。继续并联这两路的收益很小，因此没有设为默认。

模型单独在 RTX 5090、384×256 原分辨率裁剪、4 次迭代、FP16、batch=1 下，对 4 组真实 IR 输入循环执行 24 次，P95 为 12.80 ms。这只是模型阶段，不是整个检测链，也不是 24 组独立场景。完整链路还受候选搜索、验证和预算约束；不能用模型单独耗时推断系统已达标。

**评分限度：上述是“RGB 身份位置匹配，且输出了通过当前几何检查的三维观测”的覆盖率。没有独立的左右 IR 球心标签或公制三维真值，不能视为已经证明正确的三维检出率。** 此录制已用于开发，不是独立测试集。预解码、SDK 对齐、显示、编码、球台估计、完整滤波和发布成本不在实验计时内；尚未完成实机端到端验收。个别响应超过 50 ms，不能宣称硬实时。

## 模型与可复现性

- 代码来源：[NVlabs/Fast-FoundationStereo](https://github.com/NVlabs/Fast-FoundationStereo/tree/a290ba04c1b3ad1ec41a33974a157b2917b624d4)，commit `a290ba04c1b3ad1ec41a33974a157b2917b624d4`。
- 权重来源：[NVIDIA 官方 Hugging Face 仓库](https://huggingface.co/nvidia/c-fast-foundationstereo/tree/9b446878c81ddb27593036767b29b2859d46103e)，`model_best_bp2_serialize.pth`，71,105,659 字节。
- SHA256：`7aee85948373da62b0503c2542507129a3e7cab9d97d10e6790d89512a7db214`。服务端通过镜像传输后核对了官方哈希。原计划 Google Drive 权重受下载配额限制，改用上述公开官方权重；没有将它冒充论文最快配置。
- PyTorch FP16 推理，未使用 TensorRT。缺失的 `normalize` 参数按上游函数签名默认值兼容。
- 当前结果只能说明此权重、裁剪、候选和验证组合没有改善本录制，不能推断所有学习型双目方法无效。

服务器复现命令（不占用物理相机）：

```bash
cd ~/rsy/predict_node
./test_d455_realtime.sh --deadline-ms 50
./test_d455_realtime.sh --stereo-backend ffs --deadline-ms 50
```

本轮记录位于录制目录下的 `realtime_ir_ncc_control/` 和 `realtime_ir_ffs_warm/`；入口脚本的新运行输出位于 `realtime_runs/<时间>_<PID>/`。

## 下一阶段：先建立可靠红外监督

`prepare_ir_review.py` 从两路共同漏检、单路成功、共同成功和 RGB 不可见样本分层抽取左右 IR 原始 PNG，并提供 `review.html`。全部 IR 标签初始为 `unreviewed`，不把算法球心自动填作真值。复核页显示 RGB 参考、时间差、原始/拉伸对比度 IR，允许分别标注左右可见性、球心与遮挡备注，导出及恢复 JSON。

已生成 63 组复核样本。NCC 的 99 帧未及时成功中，94 帧已有二维位置但无通过验证的双目结果，5 帧无二维检测。候选级拒绝统计包含多次重试，不能把拒绝次数当作独立漏检帧数；也不能仅凭某条检查拒绝最多，就认定应放宽它。

RGB 不可见不等于 IR 不可见；裁剪没有球不等于整幅图没有球。该工具按最近 IR 时刻离线取图，只用于人工复核，不能作为实时检测数据读取方式。

训练小型 IR 球心网络尚未启动。下一步需要先复核这些样本，再扩展运动、拖影和非球负样本，并按独立录制划分训练与测试；不能把相邻帧随机分组后声称泛化达标。训练后的网络应替换当前红外亮斑候选来源，再沿用双目几何及独立时序验证，重新做相同墙钟回放和实机端到端测试。
